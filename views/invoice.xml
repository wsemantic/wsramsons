<odoo>
    <data><!-- original odoo/addons/account/views/report_invoice.xml -->
        <record id="report_invoice_document_rs_inherit" model="ir.ui.view">
            <field name="name">report.invoice.document.rs.inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.report_invoice_document"/>
            <field name="arch" type="xml">
                <!-- Localización del código de cliente -->
                <xpath expr="//div[@id='informations']//div[@name='customer_code']" position="attributes">
                    <attribute name="class">d-flex align-items-center</attribute>
                </xpath>
                <!-- Añadir el código del comercial -->
                <xpath expr="//div[@id='informations']//div[@name='customer_code']" position="after">
                    <div>
                        <strong>Cod. comercial:</strong>
                        <p class="m-0">
                            <t t-esc="o.invoice_user_id.partner_id.ref or ''"/>
                        </p>
                    </div>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
